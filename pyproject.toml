[project]
name = "taskmgr"
version = "0.2.0"
description = "Personal cron task manager via systemd timers"
requires-python = ">=3.9"
dependencies = [
    "click>=8.0",
    "rich>=13.0",
    "textual>=8.2.7",
]

[project.scripts]
taskmgr = "taskmgr.cli:main"
